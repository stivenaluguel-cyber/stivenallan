# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/
.turbo/

# production
/build

# misc
.DS_Store
*.pem

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/

# arquivos temporários
*.tmp
*.temp
tmp/
.cache/

# env files — nunca versionar segredos (service role key, JWT secret,
# tokens de API, etc.). Padrão amplo (.env*) + exceção explícita só pro
# arquivo de exemplo com placeholders vazios.
.env*
!.env.example

# vercel
.vercel

# supabase CLI (artefatos locais, não fazem parte do schema versionado)
supabase/.temp/
supabase/.branches/

# ferramentas locais (settings/estado de máquina, não fazem parte do app)
.claude/
.claude-flow/

# typescript
*.tsbuildinfo
next-env.d.ts
