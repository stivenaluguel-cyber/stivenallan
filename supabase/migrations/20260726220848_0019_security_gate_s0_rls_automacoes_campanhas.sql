-- MARCADOR HISTORICO -- NAO EXECUTA ALTERACAO DE SCHEMA.
--
-- Versao 20260726220848, ja registrada em produção no ledger
-- supabase_migrations.schema_migrations (nome remoto: 0019_security_gate_s0_rls_automacoes_campanhas).
-- O SQL original desta migration esta arquivado em
-- supabase/legacy-migrations/, preservado apenas para consulta historica --
-- nao deve ser reexecutado em nenhum ambiente.
--
-- O efeito real deste objeto ja esta coberto pelo baseline schema-only
-- (supabase/migrations/<baseline>_baseline_schema.sql) usado para
-- reconstruir bancos novos.
--
-- Este arquivo existe apenas para que o Supabase CLI reconheca esta versao
-- como ja aplicada ao rodar "supabase db push"/"migration list" contra um
-- banco cujo ledger ja contem essa versao (caso de producao) -- sem rodar
-- nenhum SQL de verdade.

SELECT 1;
